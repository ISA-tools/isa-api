language: python

python:
      - "3.4"
      - "3.5"

# install dependencies
install: "pip install -r requirements.txt"

# command to run tests
script:
      - nosetests

branches:
    only:
      - enhance/ci
